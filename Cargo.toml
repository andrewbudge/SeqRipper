[package]
name = "seqripper"
version = "2.0.0"
edition = "2024"
description = "Fast, concurrent tool for extracting genes from GenBank files"

[[bin]]
name = "seqripper"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
rayon = "1"
thiserror = "2"
